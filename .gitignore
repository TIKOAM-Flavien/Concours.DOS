node_modules/
dist/
dist-all/
.env
.env.*
!.env.example
server/admin.db
server/admin.db-wal
server/admin.db-shm
server/uploads/

# Docker build secrets / per-env overrides (never commit).
.env.production
.env.vite.build

# Local/IDE artifacts (keep repo deployable)
.cursor/
.claude/settings.local.json
*.log
*TEMPesbuild*.out

# Audit/debug scratch (reports, reproduction scripts, instrumentation logs)
debug/
